This is the admin directory (usually under a different subdomain - admin.DOMAIN.com).
Protected with .htaccess as well as additional signin priviliges.

Usually I'll edit the .htaccess to allow based on htpasswd and htgroup files in lib/
